name: Temporary runner check

on:
  push:
    branches:
      - fix/ai-engagement-terminal-failsoft

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check runner
        run: echo runner-ok
